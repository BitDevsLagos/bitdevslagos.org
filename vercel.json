{
    "builds": [
      {
        "src": "config.toml",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "public"
        }
      }
    ],
    "installCommand": "curl -sSf https://sh.rustup.rs | sh -s -- -y && source $HOME/.cargo/env && cargo install zola",
    "buildCommand": "zola build"
  }
  